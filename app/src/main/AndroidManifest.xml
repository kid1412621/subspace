<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Subspace"
        android:usesCleartextTraffic="false"
        # <!--
        # Cleartext traffic is disabled by default for security.
        # For development against local HTTP servers or specific trusted local network devices,
        # create a network_security_config.xml file (e.g., in res/xml/)
        # and reference it here with android:networkSecurityConfig="@xml/network_security_config".
        # Example for allowing cleartext for a specific domain:
        # <network-security-config>
        #     <domain-config cleartextTrafficPermitted="true">
        #         <domain includeSubdomains="true">your.local.qbittorrent.server</domain>
        #         <domain includeSubdomains="true">192.168.1.100</domain> <!-- Or IP address -->
        #     </domain-config>
        # </network-security-config>
        # -->
        android:windowSoftInputMode="adjustResize"
        tools:targetApi="35">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Subspace">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ErrorActivity"
            android:exported="false" />
    </application>

</manifest>